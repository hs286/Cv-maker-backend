export enum Role {
  ADMIN = 'admin',
  CLIENT = 'client',
  CV_SPECIALIST = 'cvSpecialist',
}


export enum UserStatus {
  Offline = 'offline',
  Online = 'online',
}

export enum ScrapeSource {
  REED = "Reed",
  TOTAL_JOBS = "Total Jobs",
  CV_LIBRARY = "CV Library",
}